/**
 * Email templates đã được chuyển sang Google Apps Script (gas/Code.gs).
 * File này export kiểu dữ liệu payload để dùng khi gọi /api/send-email.
 *
 * Re-export từ types để tiện import trong pages:
 */
export type { SendEmailPayload, AttendanceReportRecord } from '@/types'
